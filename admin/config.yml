backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)

media_folder: "img"

collections:

  - name: "special-pages"
    label: "Pages uniques"
    delete: false # Prevent users from deleting documents in this collection
    files:
      - name: "home"
        file: "index.md"
        label: "Éditer la page d'accueil"
        fields:
          - {label: "Layout", name: "layout", widget: "hidden", default: "home"}
          - {label: "Title", name: "title", widget: "string"}
          - {label: "Permalink", name: "permalink", widget: "hidden", default: "/" }
          - label: "Slideshow"
            name: slideshow
            widget: object
            required: false
            fields:
              - label: Image
                name: slideshow-images
                widget: list
                required: false
                fields:
                  - {label: Image, name: image, widget: image}
                  - {label: Title, name: title, widget: string}
          - {label: "Body", name: "body", widget: "markdown"}

      - name: "formation-page"
        file: "formations.md"
        label: "Éditer la page des formations"
        fields:
          - {label: "Layout", name: "layout", widget: "hidden", default: "formation"}
          - {label: "Title", name: "title", widget: "string"}
          - {label: "Permalink", name: "permalink", widget: "string", default: "/formations" }
          - {label: "Body", name: "body", widget: "markdown"}

      - name: "materiel-page"
        file: "materiel.md"
        label: "Éditer la page de vente des matériels"
        fields:
          - {label: "Layout", name: "layout", widget: "hidden", default: "materiel"}
          - {label: "Title", name: "title", widget: "string"}
          - {label: "Permalink", name: "permalink", widget: "string", default: "/materiels-de-premiers-soins" }
          - {label: "Body", name: "body", widget: "markdown"}

      - name: "home"
        file: "_layouts/default.html"
        label: "Éditer les liens de navigation"
        fields:
          - {label: "Layout", name: "layout", widget: "hidden", default: "site"}
          - label: "Navigation"
            name: navigation
            widget: object
            required: false
            fields:
              - label: Liens de navigation
                name: links
                widget: list
                required: false
                fields:
                  - {label: Lien, name: link, widget: string}
                  - {label: Titre, name: title, widget: string}

  - name: "formations" # Used in routes, e.g., /admin/collections/blog
    label: "Formations" # Used in the UI
    folder: "_formations" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Titre", name: "title", widget: "string"}
      - {label: "Lien", name: "permalink", widget: "string"}
      - {label: "Image", name: "image", widget: "image", required: false}
      - {label: "Durée", name: "duration", widget: "string", required: false}
      - {label: "Prix", name: "price", widget: "string", required: false}
      - {label: "Description", name: "description", widget: "markdown"}

  - name: "materiel" # Used in routes, e.g., /admin/collections/blog
    label: "Matériel de premiers soins" # Used in the UI
    folder: "_materiel" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Titre", name: "title", widget: "string"}
      - {label: "Lien", name: "permalink", widget: "string"}
      - {label: "Image", name: "image", widget: "image", required: false}
      - {label: "Description", name: "description", widget: "markdown"}
      - {label: "Prix", name: "price", widget: "string"}


  - name: "pages" # Used in routes, e.g., /admin/collections/blog
    label: "Pages" # Used in the UI
    folder: "_pages" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Layout", name: "layout", widget: "hidden", default: "page"}
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Permalink", name: "permalink", widget: "string"}
      - {label: "Featured Image", name: "image", widget: "image", required: false}
      - {label: "Body", name: "body", widget: "markdown"}

  - name: "navigation" # Used in routes, e.g., /admin/collections/blog
    label: "Navigation" # Used in the UI
    folder: "_nav" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "{{slug}}" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - name: title
        label: Titre
        widget: relation
        collection: pages
        searchFields: [title]
        valueField: title
      - name: slug
        label: Lien
        widget: relation
        collection: pages
        searchFields: [permalink]
        valueField: permalink
      - {label: "Ordre", name: "order", widget: "number"}
